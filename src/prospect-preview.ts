import { config } from './config.js';
import type { Prospect } from './types.js';

export function renderProspectPreview(prospect: Prospect): string {
  if (!prospect.preview || !prospect.qualification) throw new Error('Campaign Preview is not ready');
  const preview = prospect.preview;
  const applyUrl = `${config.marketingUrl.replace(/\/$/, '')}/apply?ref=${encodeURIComponent(prospect.previewToken)}`;
  const sourceHost = new URL(prospect.input.sourceUrl).hostname.replace(/^www\./, '');
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow"><title>${escapeHtml(preview.guideTitle)} — Campaign Preview</title><style>
  :root{--paper:#f6f2ea;--paper2:#efe9dd;--card:#fffdf8;--ink:#1c1b18;--ink2:#2f2c26;--muted:#6b665c;--forest:#1f4d3a;--rule:rgba(28,27,24,.14)}*{box-sizing:border-box}body{margin:0;background:var(--paper);color:var(--ink);font-family:Arial,sans-serif;-webkit-font-smoothing:antialiased}.nav{height:76px;padding:0 clamp(20px,5vw,64px);display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid var(--rule)}.logo{font:400 23px Georgia,serif}.logo b{color:var(--forest)}.nav span{color:var(--muted);font-size:12px}.hero{max-width:1240px;margin:auto;padding:76px clamp(20px,5vw,64px) 100px;display:grid;grid-template-columns:minmax(0,1.1fr) minmax(300px,.65fr);gap:clamp(40px,6vw,90px);align-items:center}.eyebrow{color:var(--muted);font-size:14px;font-weight:600}h1{max-width:780px;margin:22px 0 25px;font:400 clamp(44px,5.6vw,78px)/1.04 Georgia,serif;letter-spacing:-.025em}.hero p{max-width:620px;color:var(--ink2);font-size:18px;line-height:1.65}.cover{min-height:500px;padding:36px;display:flex;flex-direction:column;justify-content:space-between;background:var(--forest);color:var(--paper);border-radius:5px;box-shadow:0 24px 60px rgba(60,50,30,.18)}.cover small{color:#b9d3c3;font-size:12px}.cover h2{margin:22px 0;font:400 36px/1.12 Georgia,serif}.cover p{color:#d5e3da;font-size:14px}.cover footer{padding-top:18px;border-top:1px solid rgba(246,242,234,.2);color:#b9d3c3;font-size:12px}.section{padding:105px max(6vw,28px);border-top:1px solid var(--rule)}.section:nth-child(even){background:var(--paper2)}.section-head{max-width:1120px;margin:0 auto 55px;display:grid;grid-template-columns:.8fr 1fr;gap:7vw}.section h2{margin:0;font:400 clamp(36px,4.5vw,58px)/1.06 Georgia,serif;letter-spacing:-.025em}.section-head p{margin:0;color:var(--muted);line-height:1.75}.section-head a{color:var(--forest)}.source{max-width:1120px;margin:auto;padding:28px 0;border-block:1px solid var(--rule);color:var(--ink2);font:italic 24px/1.5 Georgia,serif}.grid,.bundle{max-width:1120px;margin:auto;display:grid;border:1px solid var(--rule);background:var(--card);border-radius:6px;overflow:hidden}.grid{grid-template-columns:repeat(3,1fr)}.card{min-height:210px;padding:27px;border-right:1px solid var(--rule)}.card:last-child{border:0}.card span,.bundle b{color:var(--forest)}.card p{margin:70px 0 0;line-height:1.5}.bundle{grid-template-columns:repeat(4,1fr)}.bundle div{padding:28px;border-right:1px solid var(--rule)}.bundle div:last-child{border:0}.bundle b{display:block;font:400 28px Georgia,serif}.bundle span{color:var(--muted);font-size:13px}.cta{text-align:center;padding:120px 25px;background:var(--forest);color:var(--paper)}.cta h2{max-width:900px;margin:0 auto 25px;font:400 clamp(40px,5vw,64px)/1.05 Georgia,serif}.cta p{max-width:620px;margin:0 auto 32px;color:#d5e3da;line-height:1.7}.button{display:inline-block;padding:15px 23px;background:var(--paper);color:var(--forest);border-radius:3px;font-weight:700;text-decoration:none}@media(max-width:760px){.hero,.section-head{grid-template-columns:1fr}.cover{min-height:420px}.grid,.bundle{grid-template-columns:1fr}.card,.bundle div{border-right:0;border-bottom:1px solid var(--rule)}}
  </style></head><body><nav class="nav"><div class="logo">Afterword<b>.</b></div><span>Private campaign preview</span></nav><main><section class="hero"><div><div class="eyebrow">Prepared for ${escapeHtml(prospect.input.companyName)}</div><h1>${escapeHtml(preview.campaignAngle)}</h1><p>${escapeHtml(preview.whyNow)}</p></div><aside class="cover"><div><small>A practical field guide</small><h2>${escapeHtml(preview.guideTitle)}</h2><p>${escapeHtml(preview.guideSubtitle)}</p></div><footer>Concept preview · ${escapeHtml(prospect.input.companyName)}</footer></aside></section><section class="section"><div class="section-head"><h2>The source signal.</h2><p>Built from the public source <a href="${escapeAttribute(prospect.input.sourceUrl)}" rel="noreferrer">${escapeHtml(prospect.input.sourceTitle)}</a> on ${escapeHtml(sourceHost)}.</p></div><div class="source">${escapeHtml(preview.sourceMoment)}</div></section><section class="section"><div class="section-head"><h2>Three directions worth publishing.</h2><p>One central narrative, developed into useful angles without repeating the same post eight times.</p></div><div class="grid">${preview.articleAngles.map((angle, index) => `<article class="card"><span>0${index + 1}</span><p>${escapeHtml(angle)}</p></article>`).join('')}</div></section><section class="section"><div class="section-head"><h2>One source. Thirteen finished assets.</h2><p>Afterword handles editorial development, brand application, source checking, layout, and one consolidated revision.</p></div><div class="bundle"><div><b>01</b><span>Premium guide</span></div><div><b>08</b><span>LinkedIn posts</span></div><div><b>03</b><span>Nurture emails</span></div><div><b>01</b><span>Landing page</span></div></div></section><section class="cta"><h2>Turn this source into the full campaign.</h2><p>First review link within 48 hours after complete intake. $1,500 per month. No meetings required.</p><a class="button" href="${escapeAttribute(applyUrl)}">Start this campaign</a></section></main></body></html>`;
}

export function renderUnsubscribePage(companyName: string): string {
  return `<!doctype html><html><body style="margin:0;background:#f6f2ea;color:#1c1b18;font-family:Arial;display:grid;place-items:center;min-height:100vh;text-align:center"><main><p style="font:22px Georgia,serif">Afterword<span style="color:#1f4d3a">.</span></p><h1>You are opted out.</h1><p style="color:#6b665c">We will not send further outreach to ${escapeHtml(companyName)} at this address.</p></main></body></html>`;
}

function escapeHtml(value: string): string {
  return value.replace(/[&<>"']/g, (character) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#039;' })[character] ?? character);
}

function escapeAttribute(value: string): string { return escapeHtml(value); }
