import { config } from './config.js';
import type { Prospect } from './types.js';

export function renderProspectPreview(prospect: Prospect): string {
  if (!prospect.preview || !prospect.qualification) throw new Error('Campaign Preview is not ready');
  const preview = prospect.preview;
  const applyUrl = `${config.marketingUrl.replace(/\/$/, '')}/apply?ref=${encodeURIComponent(prospect.previewToken)}`;
  const sourceHost = new URL(prospect.input.sourceUrl).hostname.replace(/^www\./, '');
  const brandColor = safeColor(prospect.brand?.primaryColor);
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow"><title>${escapeHtml(preview.guideTitle)} — Campaign Preview</title><style>
  :root{--brand:${brandColor};--gold:#e8c97a;--ink:#f5f1e8;--muted:#969891;--line:rgba(255,255,255,.12);--bg:#0a0b0b}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink);font-family:Arial,sans-serif}.nav{height:72px;padding:0 5vw;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid var(--line)}.logo{display:flex;gap:11px;align-items:center;font-weight:800}.logo i{width:30px;height:30px;border:1px solid var(--gold);display:grid;place-items:center;color:var(--gold);font:italic 18px Georgia}.nav span{color:#666;font:10px monospace;text-transform:uppercase;letter-spacing:.12em}.hero{min-height:650px;padding:90px max(6vw,28px);display:grid;grid-template-columns:minmax(0,1.1fr) minmax(330px,.6fr);gap:7vw;align-items:center;background:radial-gradient(circle at 75% 25%,color-mix(in srgb,var(--brand) 14%,transparent),transparent 33%)}.eyebrow{color:var(--gold);font:700 10px monospace;text-transform:uppercase;letter-spacing:.15em}h1{max-width:820px;margin:20px 0 25px;font:400 clamp(48px,7vw,98px)/.98 Georgia;letter-spacing:-.055em}.hero p{max-width:650px;color:var(--muted);font-size:18px;line-height:1.7}.cover{min-height:500px;border:1px solid var(--line);padding:35px;display:flex;flex-direction:column;justify-content:space-between;background:#111313;box-shadow:0 35px 90px #000}.cover small{color:var(--brand);font:700 9px monospace;text-transform:uppercase;letter-spacing:.15em}.cover h2{font:400 36px/1.08 Georgia;margin:20px 0}.cover p{font-size:13px}.cover footer{border-top:1px solid var(--line);padding-top:18px;color:#666;font:9px monospace;text-transform:uppercase}.section{padding:105px max(6vw,28px);border-top:1px solid var(--line)}.section-head{display:grid;grid-template-columns:.7fr 1fr;gap:7vw;margin-bottom:55px}.section h2{margin:0;font:400 clamp(38px,5vw,66px)/1.02 Georgia;letter-spacing:-.04em}.section-head p{margin:0;color:var(--muted);line-height:1.75}.source{padding:25px 0;border-block:1px solid var(--line);color:#c8c5bd;font:italic 24px/1.5 Georgia}.grid{display:grid;grid-template-columns:repeat(3,1fr);border:1px solid var(--line)}.card{min-height:210px;padding:27px;border-right:1px solid var(--line)}.card:last-child{border:0}.card span{color:var(--gold);font:9px monospace}.card p{margin:70px 0 0;line-height:1.5}.bundle{display:grid;grid-template-columns:repeat(4,1fr);border:1px solid var(--line)}.bundle div{padding:28px;border-right:1px solid var(--line)}.bundle div:last-child{border:0}.bundle b{display:block;color:var(--gold);font-size:24px}.bundle span{color:var(--muted);font-size:12px}.cta{text-align:center;padding:130px 25px;background:var(--gold);color:#111}.cta h2{font:400 clamp(42px,6vw,76px)/1 Georgia;margin:0 auto 25px;max-width:900px}.cta p{max-width:600px;margin:0 auto 32px;line-height:1.7}.button{display:inline-block;background:#111;color:#fff;padding:16px 23px;font-weight:800;text-decoration:none}@media(max-width:760px){.hero,.section-head{grid-template-columns:1fr}.cover{min-height:420px}.grid,.bundle{grid-template-columns:1fr}.card,.bundle div{border-right:0;border-bottom:1px solid var(--line)}}
  </style></head><body><nav class="nav"><div class="logo"><i>A</i>AdForge</div><span>Private Campaign Preview</span></nav><main><section class="hero"><div><div class="eyebrow">Prepared for ${escapeHtml(prospect.input.companyName)}</div><h1>${escapeHtml(preview.campaignAngle)}</h1><p>${escapeHtml(preview.whyNow)}</p></div><aside class="cover"><div><small>A practical field guide</small><h2>${escapeHtml(preview.guideTitle)}</h2><p>${escapeHtml(preview.guideSubtitle)}</p></div><footer>Concept preview · ${escapeHtml(prospect.input.companyName)}</footer></aside></section><section class="section"><div class="section-head"><h2>The source signal.</h2><p>Built from the public source <a href="${escapeAttribute(prospect.input.sourceUrl)}" style="color:var(--gold)" rel="noreferrer">${escapeHtml(prospect.input.sourceTitle)}</a> on ${escapeHtml(sourceHost)}.</p></div><div class="source">${escapeHtml(preview.sourceMoment)}</div></section><section class="section"><div class="section-head"><h2>Three directions worth publishing.</h2><p>One central narrative, developed into useful angles without repeating the same post eight times.</p></div><div class="grid">${preview.articleAngles.map((angle, index) => `<article class="card"><span>0${index + 1}</span><p>${escapeHtml(angle)}</p></article>`).join('')}</div></section><section class="section"><div class="section-head"><h2>One source. Thirteen finished assets.</h2><p>AdForge handles editorial development, brand application, source checking, layout, and one consolidated revision.</p></div><div class="bundle"><div><b>01</b><span>Premium guide</span></div><div><b>08</b><span>LinkedIn posts</span></div><div><b>03</b><span>Nurture emails</span></div><div><b>01</b><span>Landing page</span></div></div></section><section class="cta"><h2>Turn this source into the full campaign.</h2><p>First delivery within 48 hours after complete intake. $1,500 per month. No meetings required.</p><a class="button" href="${escapeAttribute(applyUrl)}">Start this campaign →</a></section></main></body></html>`;
}

export function renderUnsubscribePage(companyName: string): string {
  return `<!doctype html><html><body style="margin:0;background:#090a0a;color:#f4f0e8;font-family:Arial;display:grid;place-items:center;min-height:100vh;text-align:center"><main><p style="color:#e8c97a;text-transform:uppercase;letter-spacing:.12em">AdForge</p><h1>You're opted out.</h1><p style="color:#8d918d">We will not send further outreach to ${escapeHtml(companyName)} at this address.</p></main></body></html>`;
}

function safeColor(value: string | undefined): string {
  if (value && /^(#[0-9a-f]{3,8}|rgba?\([\d\s.,%]+\)|hsla?\([\d\s.,%deg]+\))$/i.test(value)) return value;
  return '#e8c97a';
}

function escapeHtml(value: string): string {
  return value.replace(/[&<>"']/g, (character) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#039;' })[character] ?? character);
}

function escapeAttribute(value: string): string { return escapeHtml(value); }
